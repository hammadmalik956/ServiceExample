apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: serviceexample
  namespace: default
spec:
  createNamespace: true
  interval: 5m
  chart:
    spec:
      chart: serviceexample
      sourceRef:
        kind: HelmRepository
        name: serviceexample
        namespace: flux-system
      version: ">=0.1.0"
  values:
    image:
      repository: public.ecr.aws/a1f9o5p6/serviceexample
      tag: ""  # Flux Image Update will populate automatically
